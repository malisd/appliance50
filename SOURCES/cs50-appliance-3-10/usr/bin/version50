#!/bin/bash

declare rpmquery=$(/usr/bin/rpmquery cs50-appliance)
declare version=$(/usr/bin/expr "$rpmquery" : 'cs50-appliance-\([0-9]*-[0-9]*\)\.i386')
if [ "$version" != "" ]; then
    echo $version
fi
